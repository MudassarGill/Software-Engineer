// variables
var AccountID = "123456";
let IsLoggedIn = true;
const UserName = "JohnDoe";

var AccountID = "654321"; // reassigning variable
// if we want to change any variable name we need to add var or let but const don't work and not change if init
console.table([AccountID, IsLoggedIn, UserName]);